"use client";
import { useState } from "react";
export function BackupRestore(){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);return <section className="mt-7 rounded-xl border border-border bg-card p-4"><h2 className="text-lg font-bold">Резервная копия</h2><p className="mt-1 text-sm text-muted-foreground">Экспорт содержит только данные тренировок. Восстановление проверяет формат и заменяет данные одной транзакцией.</p><a href="/api/export/json" download className="mt-4 flex h-11 w-full items-center justify-center rounded-lg bg-primary px-4 font-semibold text-primary-foreground">Скачать JSON-копию</a><label className="mt-3 flex min-h-11 cursor-pointer items-center justify-center rounded-lg border border-border px-4 font-semibold">{busy?"Восстанавливаю…":"Восстановить из JSON"}<input className="sr-only" type="file" accept="application/json,.json" disabled={busy} onChange={async e=>{const file=e.target.files?.[0];if(!file)return;if(!confirm("Заменить текущие данные содержимым резервной копии?")){e.target.value="";return}setBusy(true);setMessage("");try{const text=await file.text();JSON.parse(text);const res=await fetch("/api/import/json",{method:"POST",headers:{"Content-Type":"application/json"},body:text});const data=await res.json();if(!res.ok)throw new Error(data.error||"Ошибка");setMessage("Данные восстановлены. Обновите страницу.")}catch(err){setMessage(err instanceof Error?err.message:"Ошибка файла")}finally{setBusy(false);e.target.value=""}}}/></label>{message&&<p role="status" className="mt-3 text-sm">{message}</p>}</section>}
