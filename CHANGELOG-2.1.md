# Переход на редакцию 2.1 жимовой программы

Редакция 2.1 не переписывает программу. Она чинит десять мест, где редакция 2.0
расходилась со своими собственными заявлениями, и добавляет то, чего в ней не было
вовсе — механизм прогрессии.

## Почему это понадобилось

При RMref 115 кг и жёстком потолке RPE 8 всё пространство планирования — тринадцать
клеток: тройки до 97,5 кг, двойки до 100, синглы до 105. Построить из этого тринадцать
восходящих циклов невозможно. Поэтому силовой блок 2.0 упирался в потолок к тринадцатому
циклу и дальше повторялся: Ц12 дословно совпадал с Ц15, Ц13 — с Ц16, а самый тяжёлый
многоповторный подход 148-го дня был легче, чем на 100-й день.

Задуманным двигателем прогрессии было обновление RMref. Но внутри силового блока таких
обновлений два, и второе приходится уже на тейпер. Оставалось одно рабочее обновление
с потолком +5 кг — максимум +4,3% роста нагрузки при заявленном результате +6…9%.
Механика плана не могла выдать то, что обещала.

## Данные программы

- `lib/program/h2-v9-v3.json` — версия `h2-v9-3.0`: 22 цикла, 88 сессий, 641 упражнение.
- `scripts/build-program-v3.mjs` — генератор. Три изменения в расчётной части:
  - RPE считается от **фактического веса на штанге**, а не от номинального процента.
    В 2.0 `rpeOf()` получал `pct`, а `kg()` округлял отдельно, и они никогда не сверялись:
    37 упражнений расходились больше чем на 0,35 п.п.
  - Верхнего зажима больше нет. В 2.0 `clamp()` приводил значение к 8 **перед записью**,
    из-за чего проверка «ни один подход выше RPE 8» не могла сработать никогда. Теперь
    генератор при выходе за потолок печатает список нарушителей и падает.
  - Ниже валидированного диапазона шкалы `targetRpe = null` вместо выдуманного числа.
    Текст заметки и записанное значение больше не расходятся.
- `scripts/verify-program-v3.mjs` — 15 групп проверок вместо 10.
- `lib/program/h2-v9-v2.json` и `v1.json` оставлены нетронутыми для истории.

## Десять правок

| № | Область | Что было в 2.0 | Что стало |
|---|---|---|---|
| 1 | Прогрессия | правила повышения веса нет ни в одном разделе | рабочая надбавка Δ |
| 2 | Силовой блок | Ц12 ≡ Ц15, Ц13 ≡ Ц16 | 13 уникальных циклов |
| 3 | Базовый замер | коридор ±5 кг, ошибка в 10 кг правится до 68-го дня | задаёт RMref свободно |
| 4 | Потолок RPE | три превышения, скрытых зажимом | ноль, проверяется независимо |
| 5 | Таблицы объёма | 4 строки из 11 неверны | пересчитаны генератором |
| 6 | Столбец RPE | 29 ячеек из 59 вне шкалы, включая −1,2 и −4,1 | цели нет там, где шкала не мерила |
| 7 | Кардио | 45 мин Z2 перед главным жимовым днём | длинная сессия перед лёгким днём |
| 8 | Ортостатика | правила подъёма со скамьи нет | раздел в гайдах и в промпте тренера |
| 9 | Реаб-блок | один набор на все 44 кардио-дня | шесть ступеней, шаг раз в 3 цикла |
| 10 | Пик Ц21 | сингл 107,5 кг = 93,5%, выше Уровня 2 | убран; ветка C возвращает 102,5 кг |

## Логика

- **`lib/program/delta.ts` (новый)** — рабочая надбавка Δ. Повышение: зелёный статус
  и последний рабочий подход на 0,5 RPE ниже цели → +2,5 кг, не чаще раза за цикл,
  не в разгрузках, коридор ±10 кг. Понижение: превышение цели на 0,5 RPE или две
  несделанные сессии подряд — работает в любом цикле, тормоз не зависит от фазы.
  На контрольной точке Δ сворачивается в новый RMref и обнуляется. Отдельно ловится
  случай «надбавка росла, а калибровочная тройка нет» — это дрейф оценок RPE,
  а не рост силы: база остаётся, Δ сбрасывается, техника пересматривается по видео.
- **`lib/recommend.ts`** — при фиксированной нагрузке больше не возвращает
  «запас есть, и это по плану, не повышай». Приложение уже вычисляло, что подход прошёл
  легче цели, и просто отбрасывало сигнал; теперь он уходит наверх флагом
  `suggestDeltaRaise`, а решение принимает `decideDelta` со всеми проверками.
- **`lib/program/rmref.ts`** — `checkpointAllowsUnboundedStep()`: базовый замер `h2-1`
  выведен из-под коридора ±5 кг. Коридор нужен, чтобы шум одного замера не раскачивал
  подтверждённую базу; у базового замера раскачивать нечего.
- **`lib/program/gates.ts`** — `actualPercentOfRmref()`: потолок уровня допуска считается
  от килограммов, а не от подписи. В 2.0 сингл Ц21 был записан как `percent: 92.5`
  при `exampleKg: 107.5`; фактически 93,5%. Шлюз сравнивал 92,5 с 92,5 и пропускал
  единственный подход программы, который обязан был остановить.
- **`lib/program/validate.ts`** — три проверки, которые зажимом обойти нельзя:
  пересчёт RPE от веса на штанге, потолок допуска в килограммах, отсутствие силовых
  циклов, повторяющих штанговую работу друг друга.
- **`lib/effort.ts`** — пятое наблюдение снизу, RPE 4. В 2.0 шкала начиналась с 6,
  а карточки содержали 29 подходов с целью ниже шести: попадание в такую цель
  невозможно было записать, любой лёгкий подход логировался как «тяжелее плана».
- **`lib/exercise-guides.ts`** — гайды прон. Y и серратус-пуша для новых ступеней
  реаб-блока, отдельный гайд «Подъём со скамьи после рабочего подхода».
- **`lib/coach/system-prompt.ts`** — надбавка Δ, потолок по штанге, ступени реаб-блока,
  порядок кардио-дней, ортостатика, ветка пика.
- **`lib/offline.ts`** — кэш редакции 2.0 добавлен в список вычищаемых.

## Приложение

- Ортостатика: после каждого рабочего подхода 80% и выше — снять штангу на упоры,
  несколько секунд лёжа, затем сесть на 30–60 секунд и только потом встать. Алимемазин —
  фенотиазин с альфа-1-блокадой, сертралин может её усиливать, и обморок рядом
  со штангой опаснее большинства рисков, которые эта программа учитывает.
- Вопросы назначившему врачу дополнены пунктом про ЭКГ и интервал QT: фенотиазин
  вместе с СИОЗС плюс нагрузка.
- Ветка теста выбирается на контрольной точке №4 (Ц20 B2, T−16), до первой сессии Ц21.
  По умолчанию — ветка B, тест-тройка без пикового сингла.

## Миграции

- `013_delta_offset.sql` — колонки `delta_offset_kg` и `delta_last_raised_cycle_key`
  в `program_state` с CHECK на коридор ±10 кг, журнал `delta_offset_events`,
  переключение активной версии на `h2-v9-3.0`.
- `014_seed_h2_v9_v3.sql` — сид программы. Ключи с префиксом `v3:`, потому что уникальные
  индексы `program_key` не версионированы. Данные редакций 1.0 и 2.0 и история сессий
  не затронуты.
- Ни одного `DELETE`, `TRUNCATE` или `DROP TABLE` во всех 14 миграциях.

## Проверка

- 127 тестов, все проходят (было 103).
- Новые файлы: `tests/program-delta.test.ts` (13 тестов на надбавку),
  `tests/program-revision-21.test.ts` (10 тестов на сами правки — каждый закрывает
  конкретный дефект 2.0, проходивший все проверки того времени).
- `node scripts/lint.mjs` — чисто.
- `node scripts/verify-program-v3.mjs` — все 15 групп пройдены.
- `pnpm typecheck` и `pnpm build` — чисто.

## Исправленные баги

1. Проверка `validate.ts` «ни один плановый подход выше RPE 8» была тавтологичной:
   генератор зажимал значение перед записью, и валидатор читал уже исправленное число.
   Три превышения (v9-4, v9-7, v9-12) проходили её молча.
2. Текст заметки писался незажатым, а `targetRpe` — зажатым. Пользователь видел
   «около 8,1» и «около 0,8» при записанных 8 и 4. Расхождение в 34 упражнениях.
3. Шлюз условного сингла проверял номинальный процент вместо веса на штанге и пропускал
   единственный подход программы, выходящий за уровень допуска.
4. Коридор ±5 кг применялся к базовому замеру, из-за чего циклы 2–4, подписанные
   «72,5%», могли фактически идти на 79% почти два месяца.
5. Шкала усилия начиналась с RPE 6 при 29 подходах с целью ниже шести — попадание
   в цель разгрузки нельзя было записать.
6. Кэш программы редакции 2.0 в localStorage не вычищался при переходе на новую версию:
   в 2.0 ту же ошибку исправили для 1.0, но список остался незакрытым.
7. `npm run db:dry-run` не проходил ни разу с момента перехода на редакцию 2.0.
   `freshDatabaseRun` переприменяет 009 и 010, сид 1.0 безусловно возвращает
   `program_state.program_version` к своему значению, а проверка ждала активную версию.
   Смысл переприменения — убедиться, что не поехали счётчики; версия вынесена в параметр.
